{
  "name": "hukpo",
  "version": "1.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ]
  },
  "scripts": {
    "p": "yarn workspace @hukpo/playground",
    "reset": "find . -type dir -name node_modules | xargs rm -rf"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
